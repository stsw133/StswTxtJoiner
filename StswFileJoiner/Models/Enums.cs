namespace StswFileJoiner;

public enum FileFilterMode
{
	Only,
	Excluded
}

public enum ImageJoinMode
{
	Horizontal,
	Vertical,
	Grid,
	Overlay
}

public enum ImageGridDefinitionMode
{
	Columns,
	Rows
}
